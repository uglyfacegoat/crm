"use client";

import { Check, KeyRound, LoaderCircle, Plus, ShieldCheck } from "lucide-react";
import { useActionState, useCallback, useEffect, useMemo, useState } from "react";
import { useRouter } from "next/navigation";
import { configureWebsiteIntegrationAction, createWebsiteAction, type WebsiteMutationState } from "@/app/(workspace)/sites/actions";
import { Dialog } from "@/components/ui/dialog";
import { clientCrypto as crypto } from "@/lib/client-id";
import { providerLabels } from "@/components/sites/site-card";
import { websiteProviders, type WebsiteListItem, type WebsiteProvider } from "@/server/sites/types";

const initialState: WebsiteMutationState = { status: "idle", message: null, fieldErrors: {} };
function FieldError({ errors }: { errors?: string[] }) { return errors?.[0] ? <span className="text-[10px] text-[#e18a8e]">{errors[0]}</span> : null; }

function CreateWebsiteForm({ requestKey, onComplete }: { requestKey: string; onComplete: () => void }) {
  const [state, action, pending] = useActionState(createWebsiteAction, initialState);
  const router = useRouter();
  useEffect(() => { if (state.status !== "success") return; router.refresh(); const timeout = window.setTimeout(onComplete, 700); return () => window.clearTimeout(timeout); }, [onComplete, router, state.status]);
  return <form action={action} className="flex flex-1 flex-col"><input type="hidden" name="idempotencyKey" value={requestKey} /><div className="flex-1 space-y-5 p-5 sm:p-7"><div className="rounded-[14px] border border-[var(--accent)]/12 bg-[var(--accent)]/[0.035] p-4"><p className="flex items-center gap-2 text-xs font-medium text-[#b8f7e4]"><ShieldCheck className="size-4 text-[var(--accent)]" />Сначала реестр, потом доступы</p><p className="mt-2 text-[10px] leading-5 text-[#778188]">Добавление сайта не подключает аналитику автоматически и не запрашивает токены. Источник данных настраивается отдельно.</p></div><label className="grid gap-2 text-[10px] text-[#7b858b]"><span>Название сайта *</span><input name="name" required minLength={2} maxLength={200} placeholder="ДезСервис Москва" className="focus-ring h-12 rounded-[12px] border border-white/[0.08] bg-black/15 px-3.5 text-sm text-white outline-none placeholder:text-[#566067]" /><FieldError errors={state.fieldErrors.name} /></label><label className="grid gap-2 text-[10px] text-[#7b858b]"><span>Домен *</span><input name="domain" required maxLength={253} autoCapitalize="none" spellCheck={false} placeholder="dez-service.ru" className="focus-ring h-12 rounded-[12px] border border-white/[0.08] bg-black/15 px-3.5 font-mono text-sm lowercase text-white outline-none placeholder:text-[#566067]" /><span className="text-[9px] text-[#566168]">Без `https://`, пути и параметров.</span><FieldError errors={state.fieldErrors.domain} /></label>{state.message ? <p role="status" className={`rounded-[12px] border p-3 text-xs leading-5 ${state.status === "success" ? "border-[#b8f7e4]/20 bg-[#b8f7e4]/[0.05] text-[#8ed7b8]" : "border-[#ef646a]/20 bg-[#ef646a]/[0.05] text-[#d89599]"}`}>{state.status === "success" ? <Check className="mr-2 inline size-4" /> : null}{state.message}</p> : null}</div><footer className="sticky bottom-0 flex gap-2 border-t border-white/[0.07] bg-[#25272c]/94 p-4 backdrop-blur-xl sm:px-7"><button type="button" onClick={onComplete} disabled={pending} className="focus-ring h-12 flex-1 rounded-[12px] border border-white/[0.08] text-xs text-[#8b959b]">Отмена</button><button type="submit" disabled={pending || state.status === "success"} className="focus-ring flex h-12 flex-[1.4] items-center justify-center gap-2 rounded-[12px] bg-[var(--accent)] text-xs font-semibold text-[#25272c] disabled:opacity-65">{pending ? <><LoaderCircle className="size-4 animate-spin" />Сохраняем…</> : "Добавить сайт"}</button></footer></form>;
}

export function CreateWebsiteButton({ canWrite }: { canWrite: boolean }) {
  const [requestKey, setRequestKey] = useState<string | null>(null);
  const close = useCallback(() => setRequestKey(null), []);
  return <>{canWrite ? <button type="button" onClick={() => setRequestKey(crypto.randomUUID())} className="focus-ring flex h-11 items-center justify-center gap-2 rounded-[13px] bg-[var(--accent)] px-4 text-sm font-semibold text-[#25272c]"><Plus className="size-4" />Подключить сайт</button> : null}<Dialog open={requestKey !== null} onClose={close} title="Новый сайт" description="Добавьте домен в рабочий контур организации.">{requestKey ? <CreateWebsiteForm requestKey={requestKey} onComplete={close} /> : null}</Dialog></>;
}

function ConfigureIntegrationForm({ site, requestKey, onComplete }: { site: WebsiteListItem; requestKey: string; onComplete: () => void }) {
  const configured = useMemo(() => new Set(site.integrations.map((integration) => integration.provider)), [site.integrations]);
  const available = websiteProviders.filter((provider) => !configured.has(provider));
  const [provider, setProvider] = useState<WebsiteProvider | null>(available[0] ?? null);
  const [state, action, pending] = useActionState(configureWebsiteIntegrationAction, initialState);
  const router = useRouter();
  useEffect(() => { if (state.status !== "success") return; router.refresh(); const timeout = window.setTimeout(onComplete, 700); return () => window.clearTimeout(timeout); }, [onComplete, router, state.status]);
  if (!provider) return <div className="grid flex-1 place-items-center p-8 text-center"><div><Check className="mx-auto size-7 text-[#b8f7e4]" /><p className="mt-3 text-sm font-medium text-white">Все четыре источника уже настроены</p><button type="button" onClick={onComplete} className="focus-ring mt-5 h-10 rounded-[11px] border border-white/[0.08] px-5 text-xs text-[#8d979c]">Закрыть</button></div></div>;
  return <form action={action} className="flex flex-1 flex-col"><input type="hidden" name="idempotencyKey" value={requestKey} /><input type="hidden" name="websiteId" value={site.id} /><input type="hidden" name="provider" value={provider} /><div className="flex-1 space-y-6 p-5 sm:p-7"><fieldset><legend className="text-[10px] font-semibold uppercase tracking-[0.14em] text-[#667077]">Источник данных</legend><div className="mt-3 grid grid-cols-2 gap-2">{available.map((value) => <button key={value} type="button" onClick={() => setProvider(value)} className={`focus-ring min-h-12 rounded-[12px] border px-3 text-[10px] ${provider === value ? "border-[var(--accent)]/35 bg-[var(--accent)]/[0.07] text-white" : "border-white/[0.07] text-[#7c868c]"}`}>{providerLabels[value]}</button>)}</div><FieldError errors={state.fieldErrors.provider} /></fieldset><label className="grid gap-2 text-[10px] text-[#7b858b]"><span>ID счётчика или ресурса *</span><input name="propertyId" required maxLength={200} placeholder={provider === "yandex_metrica" ? "12345678" : provider === "ga4" ? "properties/123456789" : "sc-domain:example.ru"} className="focus-ring h-12 rounded-[12px] border border-white/[0.08] bg-black/15 px-3.5 font-mono text-sm text-white outline-none placeholder:text-[#566067]" /><FieldError errors={state.fieldErrors.propertyId} /></label><label className="grid gap-2 text-[10px] text-[#7b858b]"><span>Ссылка на серверный секрет *</span><div className="relative"><KeyRound className="pointer-events-none absolute left-3.5 top-4 size-4 text-[#606b72]" /><input name="secretReference" required pattern="[A-Z][A-Z0-9_]{2,127}" maxLength={128} autoCapitalize="characters" spellCheck={false} placeholder="YANDEX_METRICA_MAIN_TOKEN" className="focus-ring h-12 w-full rounded-[12px] border border-white/[0.08] bg-black/15 pl-10 pr-3.5 font-mono text-xs uppercase text-white outline-none placeholder:text-[#566067]" /></div><span className="text-[9px] leading-4 text-[#566168]">Введите имя Docker Secret/Vault/env-переменной, а не сам OAuth-токен.</span><FieldError errors={state.fieldErrors.secretReference} /></label><div className="rounded-[12px] border border-[#b8f7e4]/12 bg-[#b8f7e4]/[0.03] p-3 text-[10px] leading-5 text-[#779186]">После сохранения источник получит статус «Ожидает». Статус «Подключён» выставит только worker после успешного запроса к API.</div>{state.message ? <p role="status" className={`rounded-[12px] border p-3 text-xs leading-5 ${state.status === "success" ? "border-[#b8f7e4]/20 bg-[#b8f7e4]/[0.05] text-[#8ed7b8]" : "border-[#ef646a]/20 bg-[#ef646a]/[0.05] text-[#d89599]"}`}>{state.message}</p> : null}</div><footer className="sticky bottom-0 flex gap-2 border-t border-white/[0.07] bg-[#25272c]/94 p-4 backdrop-blur-xl sm:px-7"><button type="button" onClick={onComplete} disabled={pending} className="focus-ring h-12 flex-1 rounded-[12px] border border-white/[0.08] text-xs text-[#8b959b]">Отмена</button><button type="submit" disabled={pending || state.status === "success"} className="focus-ring flex h-12 flex-[1.4] items-center justify-center gap-2 rounded-[12px] bg-[var(--accent)] text-xs font-semibold text-[#25272c] disabled:opacity-65">{pending ? <><LoaderCircle className="size-4 animate-spin" />Сохраняем…</> : "Сохранить подключение"}</button></footer></form>;
}

export type WebsiteIntegrationSelection = { site: WebsiteListItem; requestKey: string };

export function ConfigureIntegrationDialog({ selection, onClose }: { selection: WebsiteIntegrationSelection | null; onClose: () => void }) {
  const site = selection?.site ?? null;
  return <Dialog open={site !== null} onClose={onClose} title={site ? `Подключения · ${site.name}` : "Подключения"} description="CRM хранит ссылку на секрет, но никогда не возвращает учётные данные в браузере.">{selection ? <ConfigureIntegrationForm key={selection.requestKey} site={selection.site} requestKey={selection.requestKey} onComplete={onClose} /> : null}</Dialog>;
}
