# CRM

Восстановленная рабочая копия, 21 сентября 2026.

## Запуск

```sh
docker compose up -d --build --wait
```

Приложение: http://localhost:3000. Используются прежние база данных, документы и резервные копии Docker. Конфигурация сервисов находится в `compose.yaml`, локальные секреты — в игнорируемых Git файлах `.env.docker-*`. Их нельзя публиковать.

## План работ

`PEREDVIDEOPOKAZOM/END_PRODUCTION_PLAN.md` — последняя найденная версия из истории VS Code: оценка 6.7/10, 23 этапа и 197 пунктов. Код восстановлен из контекста Docker от 20 сентября 21:20. План новее исходников: отметки о более поздних изменениях требуют сверки с кодом, они не являются результатами текущей проверки.

Проверка восстановленного кода: 200 тестов, lint, production build и TypeScript прошли. Все 54 миграции совпали с рабочей БД.

Материалы восстановления и дамп перед обновлением сохранены в `../../crm-recovery-2026-09-21`. Исходный большой комплект с образом диска Docker остаётся в `~/frontend-backend/crm-recovery-2026-09-21`.
